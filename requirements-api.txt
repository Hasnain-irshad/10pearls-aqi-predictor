# ============================================================
#  Dependencies for the FastAPI backend (serves forecasts + on-demand predict).
#  Installs the core pipeline deps plus the web server.
# ============================================================
-r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
# AI advisor (optional) — LLM chatbot + MCP server
anthropic>=0.40
mcp>=1.0

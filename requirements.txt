# ============================================================
#  Core dependencies for the feature & training pipelines.
#  Kept lean so GitHub Actions installs fast.
#  (Streamlit app -> requirements-app.txt, deep learning -> requirements-dl.txt)
# ============================================================

# Data & numerics
pandas>=2.1,<2.3
numpy>=1.26,<2.1
pyarrow>=15.0          # Parquet read/write for the local feature store
requests>=2.31
python-dotenv>=1.0

# Feature Store + Model Registry
hopsworks>=4.0,<5.0

# Classical ML
scikit-learn>=1.4,<1.6
xgboost>=2.0,<3.0
joblib>=1.3

# Explainability
shap>=0.44

# Plotting (EDA + report figures)
matplotlib>=3.8
seaborn>=0.13
